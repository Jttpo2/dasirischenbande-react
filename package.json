{
  "name": "dasirischenbande-react",
  "version": "0.2.0",
  "private": true,
  "dependencies": {
    "color": "^3.0.0",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-scripts": "1.1.4",
    "react-social-icons": "^3.0.0",
    "styled-components": "^3.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploydev": "git co master && git merge dev && git push && git push --tags && git co dev",
    "deploy": "git co master && git push && git push --tags && git co dev"
  }
}
